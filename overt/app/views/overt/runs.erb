<span><a href="/overt">Home</a></span>
<span>/</span>
<span>Runs</span>

<table>
    <thead>
    <tr>
        <td>Run</td>
        <td>Created At</td>
    </tr>
    </thead>
    <tbody>
        <% @runs.each do |run| %>
          <tr>
            <td>
              <a href="/overt/runs/view/<%=h run.id %>">
                <%=h run.id %>
              </a>
            </td>
            <td>
              <%=h run.created_at %>
            </td>
          </tr>
        <% end %>
    </tbody>
<table>

<%= will_paginate @runs %> (<%= @runs.count %> run<%= @runs.count == 1 ? '' : 's' %>)