<script type="text/javascript">
  document.addEventListener("DOMContentLoaded", function() {
    jQuery.ajax({
      url: "/overt/crons/json/<%=h @workflow_id %>/<%=h @step_id %>"
    }).done(function(data) {
      var data = data.step.config.default_event ? data.step.config.default_event : {};
      var eventEditor = JsonEditor.create( { id: 'cron', data: data } );
    });
});
</script>

<span><a href="/overt">Home</a></span>
<span>/</span>
<span><a href="/overt/crons">Crons</a></span>
<span>/</span>
<span><%=h @step[:name] %></a></span>

<div id="cron"></div>