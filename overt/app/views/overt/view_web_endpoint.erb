<script type="text/javascript">
  document.addEventListener("DOMContentLoaded", function() {
    jQuery.ajax({
      url: "/overt/web-endpoints/json/<%=h @workflow_id %>/<%=h @step_id %>"
    }).done(function(data) {
      var data = data.step.config;
      var eventEditor = JsonEditor.create( { id: 'web_endpoint', data: data } );
    });
});
</script>

<span><a href="/overt">Home</a></span>
<span>/</span>
<span><a href="/overt/web-endpoints">Web Endpoints</a></span>
<span>/</span>
<span><%=h @step[:name] %></a></span>

<div id="web_endpoint"></div>