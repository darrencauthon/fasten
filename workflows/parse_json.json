{
  "context": {
  },
  "steps": [
    {
      "id": "get_some_json",
      "name": "Ask For Some JSON",
      "label": "Ask For Some JSON",
      "type": "ManualStart",
      "parent_step_ids": [

      ],
      "config": {
      }
    },
    {
      "id": "parse_the_Json",
      "name": "Parse the JSON",
      "label": "Parse the JSON",
      "type": "JsonParser",
      "parent_step_ids": [
        "fetch_the_Json"
      ],
      "config": {
        "path": "body"
      }
    },
    {
      "id": "fetch_the_Json",
      "name": "Fetch the JSON",
      "label": "Fetch the JSON",
      "type": "WebRequest",
      "parent_step_ids": [
        "get_some_json"
      ],
      "config": {
        "url": "{{url}}"
      }
    },
    {
      "id": "break_out_the_events",
      "name": "Break out the events",
      "label": "Break out the events",
      "type": "Splitter",
      "parent_step_ids": [
        "parse_the_Json"
      ],
      "config": {
        "path": "items"
      }
    },
    {
      "id": "add_more_data",
      "name": "add_more_data",
      "label": "add_more_data",
      "type": "Splitter",
      "parent_step_ids": [
        "break_out_the_events"
      ],
      "config": {
        "path": "topics",
        "merge_mode": "merge"
      }
    },
    {
      "id": "remove_t",
      "name": "Remove T",
      "label": "Remove T",
      "type": "EventFormatter",
      "parent_step_ids": [
        "add_more_data"
      ],
      "config": {
        "instructions": {
          "topics": "",
          "unique_id": "{{categoryId}}_{{topicId}}"
        },
        "merge_mode": "merge"
      }
    },
    {
      "id": "insert_the_record",
      "name": "Insert the record",
      "label": "Insert the record",
      "type": "CrudInsert",
      "parent_step_ids": [
        "remove_t"
      ],
      "config": {
        "record_id": "{{unique_id}}",
        "collection": "Events",
        "name": "{{title}}"
      }
    }
  ],
  "id": "parse_json"
}