{
  "context": {
  },
  "steps": [
    {
      "id": "get_some_json",
      "name": "Ask For Some JSON",
      "label": "Ask For Some JSON",
      "type": "ManualStart",
      "parent_step_ids": [

      ],
      "config": {
      },
      "message": "Manual Start for {{url}}"
    },
    {
      "id": "parse_the_Json",
      "name": "Parse the JSON",
      "label": "Parse the JSON",
      "type": "JsonParser",
      "parent_step_ids": [
        "fetch_the_Json"
      ],
      "config": {
        "path": "body"
      },
      "message": "Parse the JSON from {{url}}"
    },
    {
      "id": "fetch_the_Json",
      "name": "Fetch the JSON",
      "label": "Fetch the JSON",
      "type": "WebRequest",
      "parent_step_ids": [
        "get_some_json"
      ],
      "config": {
        "url": "{{url}}"
      },
      "message": "Fetching the code from {{url}}"
    },
    {
      "id": "break_out_the_events",
      "name": "Break out the events",
      "label": "Break out the events",
      "type": "Splitter",
      "parent_step_ids": [
        "parse_the_Json"
      ],
      "config": {
        "path": "items"
      },
      "message": "Extract {{name}}"
    },
    {
      "id": "add_more_data",
      "name": "add_more_data",
      "label": "add_more_data",
      "type": "Splitter",
      "parent_step_ids": [
        "break_out_the_events"
      ],
      "config": {
        "path": "topics",
        "merge_mode": "merge"
      }
    },
    {
      "id": "remove_t",
      "name": "Remove T",
      "label": "Remove T",
      "type": "EventFormatter",
      "parent_step_ids": [
        "add_more_data"
      ],
      "config": {
        "instructions": {
          "topics": ""
        },
        "merge_mode": "merge"
      }
    },
    {
      "id": "insert_the_record",
      "name": "Insert the record",
      "label": "Insert the record",
      "type": "CrudInsert",
      "parent_step_ids": [
        "remove_t"
      ],
      "config": {
        "record_id": "{{categoryId}}_{{topicId}}",
        "collection": "Events",
        "name": "{{title}}"
      }
    }
  ],
  "id": "parse_json"
}